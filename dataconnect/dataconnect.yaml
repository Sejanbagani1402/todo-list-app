specVersion: "v1"
serviceId: "todo-list-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-list-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
